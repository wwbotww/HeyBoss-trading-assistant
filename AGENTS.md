# 项目协作约束

`docs/project-context.md` 是本项目的需求与架构事实来源。开始任何实现前必须完整阅读该文件。

- 每个里程碑编码前先向用户提交文件清单、关键接口和验收方式，获得确认后再修改代码。
- `signals/` 只能包含无 IO、无全局状态、无 NautilusTrader 依赖的纯函数。
- 唯一下单链路是 TradeSignalEvent → execution → risk → approval → NautilusTrader 执行客户端。
- 凭据只允许来自环境变量，禁止提交 `.env`、数据库、catalog 数据或账户信息。
- Python 代码必须有完整类型注解，并通过 ruff、mypy strict 与 pytest。
- 代码注释和项目文档使用中文，运行日志使用英文。

## 代码演进约束

- 动手前先读相关的现有代码；优先复用和修改现有函数，禁止通过新增相似函数或复制粘贴来绕开对旧代码的改动。
- 采用改动面最小的方案；若现有结构容纳不了新功能，先做一次不改行为的重构提交为其让路，再提交功能代码。
- 禁止投机性设计：不写只有一个实现的抽象、没有调用方的函数、"以后可能用到"的参数；同一模式重复第三次时才提取抽象。
- 被替代的代码直接删除，禁止注释保留或 `_old`/`_v2` 后缀；删除代码与新增代码同等算作产出。
- 新增第三方依赖前必须说明理由并获得用户批准。
- 当你认为某条约束或现有架构本身是问题的根源时，停止实现，说明理由并提出结构性建议，等待用户裁决。